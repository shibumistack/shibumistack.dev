# Plans

## Active

- [ ] Add protected-branch PR flow to owned ship client, including exact merged-SHA handoff.
- [ ] Bootstrap Vibetoolbox and MCPVault owned clients to v27; verify automatic update on next release.

## Planned

- [ ] Move `shibumistack.dev` from legacy VPS builds to Shibumi prebuilt deployment.
- [ ] Build full task-oriented shibumi-server documentation beyond `/server` and `/ship`.
- [ ] Add webhook adapters for git hosts beyond GitHub.

## Recently shipped

- [x] Ship v27 reviewed automatic client updates after successful deployments.
- [x] Ship v25 stale local upload-tag replacement while retaining BuildKit cache.
- [x] Ship v24 generic OCI identity labels, strict server verification, and `--rebuild`.
- [x] Publish and deploy `shibumi-server` 0.7.11.
- [x] Add and benchmark Mac-built ARM64 image uploads over SSH.
